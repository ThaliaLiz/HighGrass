[gd_resource type="ShaderMaterial" load_steps=2 format=2]

[sub_resource type="Shader" id=3]
code = "shader_type canvas_item;

void vertex() {
	VERTEX.x += cos(TIME) * (VERTEX.y - 34.0) * 0.05;
}"

[resource]
shader = SubResource( 3 )
